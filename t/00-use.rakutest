use v6.d;
use Test;

# Will need at least this Core version to use these game modules
use MUGS::Core:ver<0.1.1>;

use MUGS::Games;


### GAME CLIENT AND SERVER IMPLEMENTATIONS

use MUGS::Server::Genre::TurnBased;

use MUGS::Server::Genre::Guessing;
use MUGS::Server::Game::NumberGuess;
use MUGS::Server::Game::Snowman;

use MUGS::Server::Genre::BoardGame;
use MUGS::Server::Genre::RectangularBoard;
use MUGS::Server::Genre::MNKGame;
use MUGS::Server::Game::TicTacToe;

use MUGS::Server::Genre::IF;
use MUGS::Server::Game::Adventure::Locations;
use MUGS::Server::Game::Adventure::Loader;
use MUGS::Server::Game::Adventure::CommandParser;
use MUGS::Server::Game::Adventure;

use MUGS::Server::Game::PFX;

use MUGS::Client::Genre::TurnBased;

use MUGS::Client::Genre::Guessing;
use MUGS::Client::Game::NumberGuess;
use MUGS::Client::Game::Snowman;

use MUGS::Client::Genre::BoardGame;
use MUGS::Client::Genre::MNKGame;
use MUGS::Client::Game::TicTacToe;

use MUGS::Client::Genre::IF;
use MUGS::Client::Game::Adventure;

use MUGS::Server::Game::PFX;


pass "all modules loaded successfully";


done-testing;
